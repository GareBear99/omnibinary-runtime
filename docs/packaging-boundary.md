# Packaging Boundary

This package can be made stronger indefinitely, but packaging alone does not complete the runtime core.

The cutoff between packaging work and runtime work is crossed only when a real guest decoder emits runnable translated code through the backend.
