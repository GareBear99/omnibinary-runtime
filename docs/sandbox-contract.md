# Sandbox Contract

A brokered execution plane is usable only when it:
- separates controller and guest execution processes
- constrains filesystem and process access
- records deterministic crash receipts
- blocks direct privilege escalation paths
- exposes a minimal capability contract
