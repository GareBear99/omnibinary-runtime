# Decoder Contract

A real decoder milestone is complete only when one guest ISA block is decoded into canonical operations with:
- deterministic guest address accounting
- explicit invalid op handling
- block termination classification
- architecture-tagged tests and fixtures
- receipts that distinguish decoder failure from unsupported input
