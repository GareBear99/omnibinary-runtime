# Preflight Flow

`obi preflight <path>` gives a concise readiness view for one target input.

It answers:
- which lane would be selected now
- whether the target is runnable now
- which blockers remain
- which commands should be run next

This is a support and operator workflow aid, not proof of universal execution support.
