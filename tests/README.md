# Tests

The repo includes lightweight source-level tests where possible.

Run locally:

```bash
cargo test
```

Important: this packaging environment does not include Cargo, so tests were not executed here.
